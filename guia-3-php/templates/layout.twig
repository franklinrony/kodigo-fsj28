<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Ejercicios PHP{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    
    <style>
        .navbar-brand {
            font-weight: bold;
        }
        .card {
            transition: transform 0.2s;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .algorithm-card {
            border-left: 4px solid #007bff;
        }
        .step-list {
            max-height: 300px;
            overflow-y: auto;
        }
        .array-display {
            background: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 0.375rem;
            padding: 1rem;
            font-family: 'Courier New', monospace;
        }
        .footer {
            background-color: #f8f9fa;
            border-top: 1px solid #dee2e6;
        }
    </style>
    
    {% block extra_css %}{% endblock %}
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-code-slash"></i>
                Ejercicios PHP
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">  
                    <li class="nav-item">
                        <a class="nav-link" href="/ejercicios">
                            <i class="bi bi-puzzle"></i> Ejercicios
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenido principal -->
    <main class="container my-4">
        {% block content %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="footer mt-5 py-4">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <h5>Ejercicios de Algoritmos</h5>
                    <p class="text-muted">
                        Proyecto educativo implementado en PHP con programación funcional, 
                        autoloading PSR-4 y Twig con Bootstrap.
                    </p>
                </div>
                <div class="col-md-6 text-md-end">
                    <h6>Tecnologías utilizadas:</h6>
                    <ul class="list-unstyled text-muted">
                        <li><i class="bi bi-check-circle"></i> PHP 7.4+</li>
                        <li><i class="bi bi-check-circle"></i> Composer</li>
                        <li><i class="bi bi-check-circle"></i> Twig Templates</li>
                        <li><i class="bi bi-check-circle"></i> Bootstrap 5</li>
                    </ul>
                </div>
            </div>
            <hr>
            <div class="text-center text-muted">
                <small>&copy; 2025 Desarrollado pro Franklin Rony Cortez para  Bootcamp fsj28- GUIA 3&4 Algoritmos PHP</small>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    {% block extra_js %}{% endblock %}
</body>
</html> 